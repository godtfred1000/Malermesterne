
const menuBtn=document.querySelector('.menu'); const nav=document.querySelector('.nav'); if(menuBtn){menuBtn.onclick=()=>nav.classList.toggle('open')} document.querySelectorAll('.nav a').forEach(a=>a.onclick=()=>nav.classList.remove('open')); document.querySelectorAll('[data-year]').forEach(x=>x.textContent=new Date().getFullYear());
if(document.querySelector('#membersGrid')){const grid=document.querySelector('#membersGrid'),input=document.querySelector('#memberSearch'),count=document.querySelector('#memberCount'); function render(){const q=(input.value||'').toLowerCase();const data=window.MEMBERS.filter(m=>Object.values(m).join(' ').toLowerCase().includes(q));count.textContent=data.length+' medlemsbedrifter';grid.innerHTML=data.map(m=>`<article class="member"><h3>${m.name}</h3><p>📍 ${m.address}</p><p>☎ <a href="tel:${m.phone.replace(/\s/g,'')}">${m.phone}</a></p><p>✉ <a href="mailto:${m.email}">${m.email}</a></p>${m.web?`<p>↗ <a href="${m.web}" target="_blank" rel="noopener">Besøk nettsiden</a></p>`:''}</article>`).join('')} input.addEventListener('input',render);render();}
